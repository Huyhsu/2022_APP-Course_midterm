import React from "react";
import { Box, HStack, Text } from "native-base";

const CalendarScreen = ({ navigation }) => {
  return (
    <Box>
      <Text>I am CalendarScreen</Text>
    </Box>
  );
};

export default CalendarScreen;
